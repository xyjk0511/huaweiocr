# HuaweiOCR

Quick start (Windows):

1) Create a file named `.env` in the project root:
```
API_KEY=your_api_key_here
```

2) Double-click `start.bat`

Notes:
- The repository does not include `.env`, so the API key is not exposed.
- Share `.env` privately only with people you trust.
